<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bebosemsemcom.ibrahimbebo.tshtebat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/house"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/house"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WorkerDataActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WorkerActivity" />
        <activity android:name=".WorkerbaseActivity" />
        <activity android:name=".OwnerbaseActivity" />
        <activity android:name=".BublishActivity" />
        <activity android:name=".WorkerSearchActivity" />
        <activity android:name=".ToolBarActivity" />
    </application>

</manifest>